<!DOCTYPE html>
<html lang="zh">
  <head>
    <meta charset="UTF-8" />
    <meta name="author" content="Claire Chou" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="index, follow" />
    <meta name="rating" content="General" />
    <meta name="description" content="" />
    <base target="_self" />
    <link rel="shortcut icon" href="" />
    <link rel="bookmark" href="" />
    <link rel="stylesheet" href="/stylesheets/bootstrap.css">
    <title>Accounting</title>
    <script
      src="{{fontawesome}}"
      crossorigin="anonymous"
    ></script>
  </head>
  <body>
    <nav class="navbar bg-body-tertiary navbar-expand-lg bg-warning">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">
          {{#if isAuthenticated}}
            {{user.name}}
            的
          {{/if}}
          記帳本
        </a>
        <form class="d-flex" role="search">
          {{#if isAuthenticated}}
            <a class="btn btn-outline-danger" href="/users/logout">登出</a>
          {{else}}
            <a class="btn btn-outline-success" href="/users/login">登入</a>
          {{/if}}
        </form>
      </div>
    </nav>
    {{{ body }}}
    <script src="/javascripts/popper.js"></script>
    <script src="/javascripts/bootstrap.js"></script>
  </body>
</html>